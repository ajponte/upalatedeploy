---

deployment_hostname: "{{ lookup('pip', '/bin/hostname') }}"
ansible_ssh_user: vagrant
ansible_ssh_private_key_file: "{{ lookup('env', 'VAGRANT_HOME') | default(lookup('env', 'HOME'), boolean=True) ~ '/.vagrant.d/insecure_private_key' }}"
